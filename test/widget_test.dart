import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    // Real widget/unit tests should replace this as features are built.
    // This exists mainly so `flutter test` has something valid to run —
    // Flutter's default `flutter create` template generates a counter-app
    // test here that references widgets that don't exist in this project,
    // which fails immediately if left in place.
    expect(1 + 1, 2);
  });
}
